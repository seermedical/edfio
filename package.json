{
  "name": "edfio",
  "version": "0.1.0",
  "description": "Read & write EDF files",
  "main": "index.node",
  "scripts": {
    "build": "cargo-cp-artifact -nc index.node -- cargo build --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "cargo test"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}